# 硬件规格

本文件描述当前唯一硬件方案：固定摄像头 + 上位机运行 YOLO + Blue Pill + SG90 的单轴水平人物指向结构。

## 视觉输入

- 摄像头：`USB 摄像头` 或 `手机摄像头`
- 安装方式：固定机位
- 处理位置：上位机
- 当前用途：人物检测、目标初始化和后续位置更新

说明：

- 当前默认使用固定摄像头，不做云台随动。
- YOLO 在上位机运行，不在嵌入式端运行。

## 上位机

- 运行环境：PC 或笔记本
- 主要任务：摄像头读取、YOLO 推理、目标选择、角度计算、串口发送
- 推荐依赖：`OpenCV`、`ultralytics`、`pyserial`

## 主控

- 主控：`Blue Pill（STM32F103C8）`
- 逻辑电压：`3.3V`
- 下载方式：`ST-LINK V2（SWD）`
- 串口调试：`CH340 USB-TTL，115200 8N1`

说明：

- Blue Pill 只负责串口协议和 SG90 执行。
- 固件不处理目标语义、不运行检测模型。

## 执行机构

- 执行机构：`SG90`
- 控制方式：Blue Pill PWM
- 中心角：`90°`
- 安全角范围：`20° ~ 160°`
- 机械输出：轻质箭头或指针

说明：

- 当前只做单轴水平指向。
- 结构上应尽量减轻箭头重量，减少抖动。

## 调试与连接

- 下载器：`ST-LINK V2`
- 串口模块：`CH340 USB-TTL`
- 供电：`独立 5V 电源，共地`

供电约束：

- SG90 不从 Blue Pill 3.3V 引脚直接取电。
- 舵机使用独立 5V 更稳妥。
- 舵机地线、Blue Pill 地线、CH340 地线必须共地。

## 当前已有硬件

- `Blue Pill`
- `STM32F103 核心板`
- `SG90`
- `ST-LINK V2`
- `CH340 USB-TTL`
- `HW-131`
- `面包板`
- `杜邦线`
- `电阻 / LED`

## 建议补充

- 固定摄像头支架
- 稳定独立 5V 供电
- 箭头固定件
- 舵机安装底座

## 当前不包含

- 多轴云台结构
- 任何用于身份识别的专用硬件
- 与当前主线无关的额外输入前端
